rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc/ghi dữ liệu công khai (dành cho quét mã QR & đồng bộ dữ liệu)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
